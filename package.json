{
  "name": "tasks-gas",
  "version": "1.2.0",
  "private": true,
  "description": "Google Sheets <-> Google Tasks <-> Google Calendar sync, with Claude-powered planner.",
  "scripts": {
    "login": "clasp login",
    "pull": "clasp pull",
    "push": "clasp push",
    "push:watch": "clasp push --watch",
    "open": "clasp open",
    "status": "clasp status",
    "logs": "clasp logs --watch"
  },
  "devDependencies": {
    "@google/clasp": "^3.3.0"
  }
}
